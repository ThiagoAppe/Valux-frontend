import { Link } from "react-router-dom"
import Hero from "../components/landing/hero"

const Landing = () => {
  return (
    <div>
      <section>
        <Hero/>
      </section>
    </div>
  )
}

export default Landing